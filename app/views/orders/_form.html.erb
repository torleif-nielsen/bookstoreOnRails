  <%= simple_form_for(@order) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :name %>
      <%= f.input :address %>
      <%= f.input :email %>
      <%= f.input :pay_type, collection: ["Online Check", "Credit Card"] %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Place Order", class:"btn btn-success btn-xl"%>
    </div>
  <% end %>
